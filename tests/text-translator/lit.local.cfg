# Don't run the text-translator tests if we don't have a text-translator
# or we don't have access to the models.
if not config.tool_to_path['text-translator'] or not config.models_dir:
    config.unsupported = True
